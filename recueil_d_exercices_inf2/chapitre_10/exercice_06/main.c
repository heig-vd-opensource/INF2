#include <stdio.h>
#include <stdlib.h>

/**
Met le bit en position pos dans la représentation binaire
du nombre n à la valeur bitValue

pos = 0 correspond à la position du bit de poids le plus faible
*/
int* setBit(unsigned short pos, unsigned short bitValue, int* n);

int main(void) {


	return EXIT_SUCCESS;
}